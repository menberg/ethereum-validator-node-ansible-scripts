all:

  children:

    localhost:
      hosts:
        ethnode:
          ansible_host: localhost
          ansible_connection: local
          ansible_user: automation_admin
          project_name: ethnode


    servers:
      hosts:

        ethnode:
          project_name: ethnode
          ansible_host: 192.168.1.101
          ansible_user: automation_admin
          ansible_ssh_private_key_file: "/var/lib/automation_admin/.ssh/id_ed25519"
          ansible_port: 22
